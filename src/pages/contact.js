import * as React from 'react';
import Layout from '../components/Layout';
const contact = () => {
    return (
        <Layout>
            <section>
                <h2>Contact </h2>
            </section>
        </Layout>
    );
};

export default contact;
