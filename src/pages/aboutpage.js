import * as React from 'react';
import Layout from '../components/Layout';
const about = () => {
    return (
        <Layout>
            <section>
                <h2>About </h2>
            </section>
        </Layout>
    );
};

export default about;
